import React from "react";
import { Button } from "./ui/button";

type Props = {};

const Demo = (props: Props) => {
  return (
    <div>
      <Button>Hello</Button>
    </div>
  );
};

export default Demo;
